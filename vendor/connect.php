<?php

    $connect = mysqli_connect('localhost', 'root', '99693411', 'myDatabase');

    // if (!$connect) {
    //     die('Error connect to DataBase');
    // } else {
    //     die('Connect to database');
    // }
    //connect to database

    // $db = new SQLite3('../database/persons.db');

    // $db->exec("INSERT INTO main.Persons ('email', 'password', 'name') VALUES ('aaa', 'bbb', 'ccc')");

    // $results = $db->query('SELECT email FROM Persons');
    // while ($row = $results->fetchArray()) {
    //     var_dump($row);
    // }

    
?>